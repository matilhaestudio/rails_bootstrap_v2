json.extract! sample, :id, :first_name, :last_name, :phone, :email, :zipcode, :street, :number, :neighborhood, :city, :state, :complement, :birthday, :picture, :cpf, :rg, :created_at, :updated_at
json.url sample_url(sample, format: :json)
